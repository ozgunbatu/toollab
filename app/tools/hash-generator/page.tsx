'use client';
import {useState} from 'react';
export default function HashGeneratorPage(){const [input,setInput]=useState('');const [hashes,setHashes]=useState<Record<string,string>>({});const gen=async()=>{const data=new TextEncoder().encode(input);const algos=['SHA-1','SHA-256','SHA-512'];const res:Record<string,string>={};for(const a of algos){const buf=await crypto.subtle.digest(a,data);res[a]=Array.from(new Uint8Array(buf)).map(b=>b.toString(16).padStart(2,'0')).join('');}setHashes(res);};return(<div className="min-h-screen py-12"><div className="max-w-4xl mx-auto px-4"><h1 className="section-heading mb-4">Hash Generator</h1><div className="card mb-6"><textarea value={input} onChange={e=>setInput(e.target.value)} placeholder="Enter text..." className="tool-textarea mb-4"/><button onClick={gen} className="btn-primary">Generate</button></div>{Object.entries(hashes).map(([a,h])=>(<div key={a} className="card mb-4"><div className="font-medium mb-2">{a}</div><div className="p-3 bg-gray-50 rounded font-mono text-xs break-all">{h}</div></div>))}</div></div>);}
