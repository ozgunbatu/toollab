'use client';
import {useState} from 'react';
export default function ColorPickerPage(){const [hex,setHex]=useState('#3b82f6');const h2r=(h:string)=>{const r=parseInt(h.slice(1,3),16),g=parseInt(h.slice(3,5),16),b=parseInt(h.slice(5,7),16);return{r,g,b}};const rgb=h2r(hex);return(<div className="min-h-screen py-12"><div className="max-w-4xl mx-auto px-4"><h1 className="section-heading mb-4">Color Picker</h1><div className="card"><input type="color" value={hex} onChange={e=>setHex(e.target.value)} className="w-full h-32 rounded-lg cursor-pointer border-2"/><div className="mt-6 space-y-4"><div className="p-4 bg-gray-50 rounded-lg"><div className="text-sm text-gray-600">HEX</div><div className="font-mono text-2xl font-bold">{hex.toUpperCase()}</div></div><div className="p-4 bg-gray-50 rounded-lg"><div className="text-sm text-gray-600">RGB</div><div className="font-mono text-2xl font-bold">rgb({rgb.r}, {rgb.g}, {rgb.b})</div></div></div></div></div></div>);}
