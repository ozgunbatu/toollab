'use client';
import {useState} from 'react';
export default function TimestampConverterPage(){const [ts,setTs]=useState(Math.floor(Date.now()/1000));const date=new Date(ts*1000);return(<div className="min-h-screen py-12"><div className="max-w-4xl mx-auto px-4"><h1 className="section-heading mb-4">Unix Timestamp Converter</h1><div className="card"><label className="block mb-2">Unix Timestamp</label><input type="number" value={ts} onChange={e=>setTs(parseInt(e.target.value))} className="tool-input mb-2"/><button onClick={()=>setTs(Math.floor(Date.now()/1000))} className="btn-secondary mb-6">Current Time</button><div className="p-4 bg-gradient-to-r from-primary-50 to-primary-100 rounded-lg"><div className="text-sm text-gray-600 mb-2">Converted Date</div><div className="text-2xl font-bold">{date.toLocaleString()}</div><div className="text-sm text-gray-600 mt-2">ISO: {date.toISOString()}</div></div></div></div></div>);}
