'use client';
import {useState} from 'react';
export default function UUIDGeneratorPage(){const [uuids,setUuids]=useState<string[]>([]);const [count,setCount]=useState(5);const gen=()=>'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g,c=>{const r=Math.random()*16|0;return(c==='x'?r:(r&0x3|0x8)).toString(16)});const generate=()=>setUuids(Array.from({length:count},()=>gen()));return(<div className="min-h-screen py-12"><div className="max-w-4xl mx-auto px-4"><h1 className="section-heading mb-4">UUID Generator</h1><div className="card"><label className="block mb-2">Count: {count}</label><input type="range" min="1" max="20" value={count} onChange={e=>setCount(parseInt(e.target.value))} className="w-full mb-4"/><button onClick={generate} className="btn-primary mb-4">Generate</button><div className="space-y-2">{uuids.map((u,i)=>(<div key={i} className="p-3 bg-gray-50 rounded font-mono text-sm flex justify-between"><span>{u}</span><button onClick={()=>navigator.clipboard.writeText(u)} className="text-primary-600">Copy</button></div>))}</div></div></div></div>);}
