'use client';
import {useState,useMemo} from 'react';
export default function WordCounterPage(){const [text,setText]=useState('');const stats=useMemo(()=>{const words=text.trim()?text.trim().split(/\s+/).length:0;const chars=text.length;const sentences=text.trim()?text.split(/[.!?]+/).filter(s=>s.trim()).length:0;return{words,chars,sentences,readTime:Math.ceil(words/200)};},[text]);return(<div className="min-h-screen py-12"><div className="max-w-4xl mx-auto px-4"><h1 className="section-heading mb-4">Word Counter</h1><div className="card mb-6"><textarea value={text} onChange={e=>setText(e.target.value)} placeholder="Type or paste text..." className="tool-textarea" style={{minHeight:'300px'}}/></div><div className="grid grid-cols-2 md:grid-cols-4 gap-4">{[{label:'Words',value:stats.words},{label:'Characters',value:stats.chars},{label:'Sentences',value:stats.sentences},{label:'Read Time',value:stats.readTime+' min'}].map((s,i)=>(<div key={i} className="card bg-gradient-to-br from-primary-50 to-white"><div className="text-sm text-gray-600 mb-1">{s.label}</div><div className="text-3xl font-bold text-primary-600">{s.value}</div></div>))}</div></div></div>);}
